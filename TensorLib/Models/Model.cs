using System;
using System.Collections.Generic;
using System.Text;

namespace TensorLib
{
    public class Model
    {
        public Model()
        {
            //a model contains layers which can be run
        }

        public Tensor Process(Tensor input)
        {
            Tensor output = new Tensor();
            return output;
        }
    }
}
