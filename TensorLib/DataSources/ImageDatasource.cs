using System;
using System.Collections.Generic;
using System.Text;

namespace TensorLib
{
    public class ImageDatasource : IDataSource
    {
        public ImageDatasource()
        {
            //breakthrough on datasources
            //the user simply loads the items into the datasource in raw format
            //images will be given as float arrays and labels
        }
    }
}
